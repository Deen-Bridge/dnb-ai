# Environment variables
.env

# Firebase service-account keys — NEVER commit these (they grant full access)
*firebase-adminsdk*.json
*service-account*.json
firebase-service-account.json

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual Environment
venv/
.venv/
ENV/
.venv_linux/
venv_linux/


# IDE
.idea/
.vscode/
*.swp
*.swo

# Logs
*.log 
# Scholar-review export (contains user questions)
data/review/
venv/
.DS_Store

# Local session store (in-memory fallback persistence)
data/store.json

# Local feedback database (SQLite fallback store)
feedback.db
feedback.db-*

# Retrieval vector index (build artifact — large, binary; rebuilt from data/)
data/retrieval_index.db
data/retrieval_index.db-*
*.retrieval.db

# Committed virtual environments are machine-specific — never commit them
venv_linux/
